{% extends 'backbase.html.twig' %}

{% block title %}Facture{% endblock %}

{% block contentback %}
    <h1>Facture</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>User</th>
            <td>{{ facture.idUser.email }}</td>
        </tr>
            <tr>
                <th>Date</th>
                <td>{{ facture.date ? facture.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PrixTotal</th>
                <td>{{ facture.prixTotal }}</td>
            </tr>
        </tbody>
    </table>


   
{% endblock %}
